local options = {
  ensure_installed = {
    ensure_installed = { "vim", "lua", "vimdoc", "html", "css", "json", "yaml", "dockerfile", "bash", "javascript",
      "typescript", "tsx", "markdown", "graphql" }
  },
  highlight = {
    enable = true
  },
  indent = {
    enable = true
  }
}

return options
